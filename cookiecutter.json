{
  "name": "My Awesome Project",
  "slug": "{{ cookiecutter.name.lower()|replace(' ', '-')|replace('.', '-')|trim() }}",
  "description": "",
  "version": "1.0.0",
  "ui_library": [
    "None",
    "Bootstrap",
    "Tailwind"
  ],
  "frontend_pipeline": [
    "None",
    "Gulp",
    "Vite"
  ],
  "has_plugins_config": "n",
  "_copy_without_render": [
    "{{ cookiecutter.slug }}/assets",
    "{{ cookiecutter.slug }}/bin",
    "{{ cookiecutter.slug }}/config",
    "{{ cookiecutter.slug }}/migrations",
    "{{ cookiecutter.slug }}/public",
    "{{ cookiecutter.slug }}/src",
    "{{ cookiecutter.slug }}/templates/layouts",
    "{{ cookiecutter.slug }}/tests",
    "{{ cookiecutter.slug }}/translations"
  ]
}
